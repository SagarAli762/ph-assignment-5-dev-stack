Dev Stack

A modern and responsive technology stack builder for developers.  
Explore different technologies and build your ideal development stack by selecting the technologies you want to use.

## Features

## 1:

Explore Technologies — Browse different technologies with their category, description, rating, and logo.

## 2:

Build Your Stack — Add, remove, and manage technologies to create your own development stack.

## 3:

Fully Responsive — Enjoy a clean and user-friendly experience on mobile, tablet, and desktop devices.

## 1. What is JSX, and why is it used in React?

Answer: JSX is a syntax extension for JavaScript that allows us to write HTML-like code inside JavaScript. It makes React UI easier to write and understand.

## 2. What is the difference between props and state?

Answer: Props are used to pass data from a parent component to a child component, while state is used to manage data that can change inside a component.

## 3. What does the useState hook do, and where did you use it in this project?

The useState hook is used to create and manage state in a React component. In my project, I used it to manage the selected technology stack and to track whether a technology has been added.

## 4. What does the useEffect hook do, and why did you need it to load the JSON data?

Answer: useEffect is used to perform side effects after a component renders.

## 5. Why does every item in a .map() list need a unique key prop?

Answer: A unique key helps React identify each item in a list and efficiently update the DOM when items are added, removed, or changed.

## 6. What is conditional rendering? Show one place you used it.

Answer: Conditional rendering means displaying different UI based on a condition. In my project, I used it to show “Your stack is empty” when no technology is selected, otherwise I display the selected technologies.

## How do you pass data from a parent component to a child component, and how does a child send something back to the parent?

Answer: We pass data from parent to child using props. To send information or trigger an action from child to parent, the parent passes a callback function as a prop, and the child calls that function.
